#Main function, called when the program starts(entry function)
int main() {
    while (true) {
        println("running loop");
        while (true) {
            println("yea");
        }
    }
    println("exited");
    return 0;
}

#TODO FIX DOUBLES, FIX BYTES IN ARGUMENTS AND TEST AROUND WITH OTHER DATA TYPES, FIX "\N" IN STRINGS