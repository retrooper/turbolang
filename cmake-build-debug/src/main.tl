int main() {
    #added while loops
    while (true) {
        println("Yes");
        #Added sleeping
        int timeInNS = 1000000000;
        #one second in nanoseconds
        sleep(timeInNS);
        #yes in nanoseconds
    }
    return 0;
}