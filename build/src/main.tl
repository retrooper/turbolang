import "io"
import "math"
int32 main() {
    #int8[100] message;
    #TODO support initializing arrays
    int32 i = 0;
    while (true) {
        printf("%s%i\n", "I: ", i);
        i = i + 1;
    }
    return 0;
}