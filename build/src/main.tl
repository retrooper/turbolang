import "io"
import "time"
#import "glfw"

i32 main() {
    #glfwInit();
    #i8* np = nullptr;
    #i32 width = 1280;
    #i32 height = 720;
    #i8* title = "Hello, World";
    #glfwCreateWindow(width, height, title, np, np);K
    while (true) {
         i64 ct = getCurrentTimeMilli();
    }
    return 0;
}