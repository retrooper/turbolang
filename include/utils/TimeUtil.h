#pragma once

#include <cstdint>
#include <chrono>

namespace turbolang {
    long getCurrentNanoTime();

    long getCurrentMicroTime();

    long getCurrentMillisTime();

    long getCurrentSecondsTime();
}